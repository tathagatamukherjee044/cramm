import { LatexDirective } from './latex.directive';

describe('LatexDirective', () => {
  it('should create an instance', () => {
    const directive = new LatexDirective();
    expect(directive).toBeTruthy();
  });
});
